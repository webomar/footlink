version: '3.7'

services:
  django-guni:
    volumes:
      - static:/static
    env_file:
      - .env
    build:
      context: .
    ports:
      - "8000:8000"
    networks:
      - ng_default
      
volumes:
  static:
    
networks:
  ng_default:
    external: true